export function update_internal_entity_from_external_item() {
  // 1. Need Internal Entity
  // 2. Need Normalized External Item Properties
  // 3 Detect field changes
  // 4. If field changes save import history item
  // 5. If no field changes, return
  // 6. If field changes, iterate through each change and handle conflict resolution
}
