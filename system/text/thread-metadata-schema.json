{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Thread Metadata",
  "description": "Schema for thread metadata.json files containing thread configuration and state",
  "type": "object",
  "required": [
    "thread_id",
    "user_public_key",
    "session_provider",
    "thread_state",
    "created_at",
    "updated_at"
  ],
  "properties": {
    "title": {
      "type": "string",
      "description": "Human-readable title for the thread (optional)"
    },
    "short_description": {
      "type": "string",
      "description": "Brief description of the thread's purpose or content (optional)"
    },
    "thread_id": {
      "type": "string",
      "format": "uuid",
      "description": "Unique identifier for the thread instance"
    },
    "user_public_key": {
      "type": "string",
      "description": "Public key of the user who owns the thread"
    },
    "workflow_base_uri": {
      "type": "string",
      "description": "Reference to the specific workflow for Base system sessions (e.g., 'sys:system/workflow/write-workflow.md')"
    },
    "session_provider": {
      "type": "string",
      "enum": ["base", "claude", "cursor", "openai"],
      "description": "Origin provider for the session - the 'engine' choice for the interaction"
    },
    "inference_provider": {
      "type": "string",
      "description": "Name of the AI provider being used (e.g., 'ollama', 'anthropic')"
    },
    "models": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Array of model names used in the thread (preferred over single model)"
    },
    "model": {
      "type": "string",
      "description": "Single model name (deprecated - use models array instead)"
    },
    "thread_state": {
      "type": "string",
      "enum": ["active", "archived"],
      "description": "Current lifecycle state of the thread"
    },
    "created_at": {
      "type": "string",
      "format": "date-time",
      "description": "Timestamp of thread initiation (ISO 8601)"
    },
    "updated_at": {
      "type": "string",
      "format": "date-time",
      "description": "Timestamp of last update (ISO 8601)"
    },
    "archived_at": {
      "type": "string",
      "format": "date-time",
      "description": "Timestamp of archival (ISO 8601) - only present if thread_state is 'archived'"
    },
    "archive_reason": {
      "type": "string",
      "enum": ["completed", "user_abandoned"],
      "description": "Reason for thread archival - 'completed' for successful completion, 'user_abandoned' for user-initiated abandonment"
    },
    "tools": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Array of tools available to this thread"
    },
    "git_branch": {
      "type": "string",
      "description": "Git branch name associated with this thread (format: 'thread/{thread_id}')"
    },
    "system_worktree_path": {
      "type": ["string", "null"],
      "description": "Path to the Git worktree for the system repository"
    },
    "user_worktree_path": {
      "type": ["string", "null"],
      "description": "Path to the Git worktree for the user repository"
    },
    "memory_repo_initialized": {
      "type": "boolean",
      "description": "Whether the thread's memory directory has been initialized as a git repository"
    },
    "message_count": {
      "type": "integer",
      "description": "Total number of messages in this thread"
    },
    "tool_call_count": {
      "type": "integer",
      "description": "Total number of tool calls made in this thread"
    },
    "user_message_count": {
      "type": "integer",
      "description": "Total number of user messages in this thread"
    },
    "assistant_message_count": {
      "type": "integer",
      "description": "Total number of assistant messages in this thread"
    },
    "input_tokens": {
      "type": "integer",
      "description": "Total number of input tokens consumed in this thread"
    },
    "output_tokens": {
      "type": "integer",
      "description": "Total number of output tokens generated in this thread"
    },
    "cache_creation_input_tokens": {
      "type": "integer",
      "description": "Total number of input tokens used for cache creation in this thread"
    },
    "cache_read_input_tokens": {
      "type": "integer",
      "description": "Total number of input tokens read from cache in this thread"
    },
    "external_session": {
      "type": "object",
      "description": "External session metadata for imported sessions",
      "properties": {
        "session_id": {
          "type": "string",
          "description": "Original session ID from the external provider"
        },
        "session_provider": {
          "type": "string",
          "enum": ["claude", "cursor", "openai"],
          "description": "External provider that originated this session"
        },
        "imported_at": {
          "type": "string",
          "format": "date-time",
          "description": "When this external session was imported into the Base system"
        },
        "provider_metadata": {
          "type": "object",
          "description": "Provider-specific metadata preserved from the original session",
          "additionalProperties": true
        },
        "raw_data_saved": {
          "type": "boolean",
          "description": "Whether the raw imported data has been saved"
        }
      },
      "required": ["session_provider"],
      "additionalProperties": false
    },
    "prompt_properties": {
      "type": "object",
      "description": "Workflow-specific properties and configuration",
      "additionalProperties": true
    },
    "tags": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Array of categorization tags following base-uri format (e.g., 'user:tag/project-alpha')"
    },
    "public_read": {
      "type": "boolean",
      "description": "Whether this thread can be read by anyone without authentication. Only affects read access - write operations are always limited to owner."
    }
  },
  "additionalProperties": false,
  "if": {
    "properties": {
      "session_provider": { "const": "base" }
    }
  },
  "then": {
    "required": ["workflow_base_uri"]
  },
  "else": {
    "required": ["external_session"]
  }
}
